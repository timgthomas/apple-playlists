{
  "name": "apple-playlists",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "test": "mocha --recursive -r @babel/register",
    "build": "parcel build",
    "prepare": "rm -rf dist && parcel build --public-url=.",
    "package": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-env": "^7.16.4",
    "@babel/register": "^7.16.0",
    "@parcel/transformer-sass": "^2.0.1",
    "alpine-test-utils": "^1.0.0",
    "mocha": "^7.2.0",
    "parcel": "^2.0.1",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "alpinejs": "^3.5.2",
    "lodash": "^4.17.21"
  }
}
